v2015-05-02
  * vim:
      增加了插件vim-gitgutter。
      修正了<C-V> 可能会转换到nopaste 模式的错误。
      修正了<C-V> 在插入模式时粘贴后光标可能会移位的错误。
      移除了插件AuthorInfo
  * nano:
      修改了中的按键映射。
      更新了语法高亮文件。
  * zsh:
      现在所有的发行版上都默认加载alias.zsh。

